# Vault symlink (each dev sets up their own; never commit notes into the repo)
/vault

# Local-only project docs (kept out of the public repo)
CLAUDE.md
ROADMAP.md

# Secrets
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Harness / agent artifacts
.claude/

# SDD process artifacts: session progress ledger and raw benchmark dumps. The curated
# design docs (docs/superpowers/specs/, docs/superpowers/plans/) stay tracked.
.superpowers/
docs/superpowers/evidence/

# Self-host OTP runtime data (jar, OSM, GTFS, built graph) — large, regenerated locally
/otp/data/

# uv lock file (generated locally; not committed)
uv.lock

# OS / editor
.DS_Store
.idea/
.vscode/
